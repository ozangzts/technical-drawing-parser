# Outputs

Parser results are written under `outputs/runs/`.

The planned `outputs/index.json` registry will map each input fingerprint to its latest processing run so the same file content is not processed repeatedly.

