# Outputs

Parser results are written under `outputs/products/`.

Developer-facing metadata is written under `outputs/internal/`.

The planned `outputs/index.json` registry will map each input fingerprint to its product JSON output so the same file content is not processed repeatedly.
