# Inputs

Place source technical drawings in `inputs/incoming/`.

The parser should leave files in this directory unchanged. Each processing run should copy the relevant source file into its own output run directory.

