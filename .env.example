# Copy this file to `.env` for local configuration.
# The default settings do not call any paid external API.

TDP_EXTRACTOR=none
TDP_MODEL=

# Required only when an OpenAI-based VLM extractor is implemented and enabled.
# OpenAI API usage may incur costs. Do not commit real secrets.
OPENAI_API_KEY=

