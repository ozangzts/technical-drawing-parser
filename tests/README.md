# Tests

Tests will live here once parser behavior is implemented.

Focus first on deterministic behavior:

- input discovery
- fingerprinting
- registry skip behavior
- manifest generation
- schema shape

